name: Deploy Astrologo Monorepo

on:
  push:
    branches: [ "main" ] # Gatilho oficial no GitHub

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Frontend Dependencies
        working-directory: ./astrologo-frontend
        run: npm install

      - name: Build Frontend
        working-directory: ./astrologo-frontend
        run: npm run build

      - name: Install Admin Dependencies
        working-directory: ./astrologo-admin
        run: npm install

      - name: Install Backend Dependencies (Functions)
        working-directory: ./functions
        run: npm install

      - name: Build Backend (Functions)
        working-directory: ./functions
        run: npm run build

      - name: Sincronizar Backend (Functions)
        run: |
          cp -r functions astrologo-frontend/
          cp -r functions astrologo-admin/

      - name: Build Admin
        working-directory: ./astrologo-admin
        run: npm run build

      - name: Deploy Frontend to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: astrologo-frontend
          directory: dist
          workingDirectory: astrologo-frontend
          branch: production # Destino oficial na Cloudflare

      - name: Deploy Admin to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: astrologo-admin
          directory: dist
          workingDirectory: astrologo-admin
          branch: production # Destino oficial na Cloudflare